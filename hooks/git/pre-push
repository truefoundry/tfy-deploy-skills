#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"

echo "[pre-push] Running skill validation..."
./scripts/validate-skills.sh

echo "[pre-push] Running local security validation..."
./scripts/validate-skill-security.sh --changed

echo "[pre-push] All checks passed."
